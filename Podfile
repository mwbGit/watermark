# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'watermark' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for watermark

  target 'watermarkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'watermarkUITests' do
    # Pods for testing
  end
  pod 'Alamofire','~> 4.8.2' 
  pod 'SwiftyJSON', '~> 4.0'
  pod 'MOBFoundation'
  pod 'SnapKit', '~> 4.2.0'
  pod 'MediaComposition'
  pod 'CLImagePickerTool'
  pod 'Bugly'
  pod 'WAVideoBox'
end
